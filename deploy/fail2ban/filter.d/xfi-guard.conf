[Definition]
# XFI Guard uses this filter only for events explicitly attributable to
# the XFI Guard defense pipeline. Do not match every sshd failure here,
# otherwise this jail would duplicate the standard sshd jail.
failregex = ^XFI-GUARD\s+THREAT\s+<HOST>\s*$
ignoreregex =
